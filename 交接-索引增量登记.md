# 交接：把「改一个词」从 14 秒压到毫秒（第 2 步 · 台账增量登记）

**一句话结论**：大文件改一个词慢，卡在三个地方——重写整份数据文件约 4 秒、把整份文件重新登记进台账约 5 秒、登记本机目录约 4.4 秒。已经修掉的是"系统调用风暴"（21.8 秒 → 14.2 秒）；剩下这三处还没动，按本文第 5 节的顺序做，目标是 ≤50 毫秒。

> 读者：下一个来干活的 session（假设它对这个仓库一无所知），以及项目维护者。
> 配套文件：`CLI现状.md`（本次一并放到本分支，含写路径时序、性能数字与「不要重做」清单）。
> 所有数字都标了口径；凡是推算都写明是推算。

## 0. 这份文档怎么用

1. 先读第 1 节（把词说清）和第 2 节（现在的情况），再看第 3 节（一次编辑要经过哪六步）。
2. 第 4 节是**已经做完、不要再做一遍**的事。
3. 第 5 节是**待做的三块**，每块按「遇到了什么问题 → 打算怎么解决 → 收益 / 成本 → 要不要人决定」写。
4. 第 6 节是**歧义检查**：两个 session 最容易互相误解的说法，逐条钉死。
5. 第 7 节是动手前必须先问清楚的问题；第 8 节是可以直接照抄的命令。

## 1. 先把词说清（零基础也能读）

| 词 | 大白话 | 它是不是缓存 |
|---|---|---|
| **数据文件**（`.xirang`） | 真正存东西的文件，看到的每个词都在里面 | 不是，是唯一真相 |
| **节点 / 编号** | 文件里的一行东西叫节点，每个节点有一个全世界唯一的编号（UUID） | — |
| **本机目录**（catalog） | 记「哪个编号在哪个文件里」的一本小册子，用来跨文件查找 | 是缓存，删了能重建 |
| **台账**（工作区索引） | 记「每个节点在文件的第几个字节」的账本，让查找不用从头翻 | 是缓存，删了能重建 |
| **侧车索引**（`.xirang.idx`） | 台账出现之前的旧做法：每个数据文件旁边放一张小账本 | 是缓存，现在默认不用 |
| **块 / 日志** | 台账由两部分组成：块是整理好的大表格，日志是新改动一条条记在后面的小纸条 | 都是台账内部 |
| **压实**（compact） | 把日志里的小纸条合并回大表格 | — |
| **留痕**（`@history`） | 每次改动把旧值抄一份存起来用于回滚，会让文件越用越大 | 不是缓存，是数据 |
| **代号**（gen） | 台账给每个数据文件的一个版本号，用来判断账本记的是不是当前状态 | — |
| **append-v1** | 一个已注册的协议：允许数据文件"只追加改动记录"，读的时候同编号取最后一条 | 协议 |

**最容易混的三个「索引」**：本机目录、台账、侧车。它们都是缓存，但记的东西不同——目录记「编号→文件」，台账记「编号→文件+字节位置」。说「索引」时一定要说清是哪一个。

## 2. 现在的情况（实测）

**口径**：release 构建；数据文件 `S4_生物.xirang`，187,780,384 字节、约 347 万节点；关闭自动整理（`XIRANG_INDEX_MAINTENANCE=off`）；单次测量。

| 操作 | 修复前 | 修复后（提交 `6d3efef`） |
|---|---|---|
| 建一次台账 | 3.93 秒 | 3.93 秒（未变） |
| `xr set` 改一个词（默认：台账 + 本机目录） | 21.83 秒 | **14.23 秒** |
| 同上，但关掉本机目录（`XIRANG_INDEX=off`） | 18.46 秒 | **9.87 秒** |

**14.23 秒拆开看**（下一步要砍的对象）：

| 花在哪 | 约耗时 | 属于 |
|---|---|---|
| 登记本机目录（把 347 万个编号写进小册子） | 4.4 秒 | 本机目录 |
| 把 187 MB 文件整份读进内存 | 1 秒 | 数据层 |
| 把 187 MB 文件整份**重写**回去 | 4 秒 | 数据层 |
| 把整份文件**重新登记**进台账（重扫 + 重编码全部条目） | 5 秒 | 台账 |

> 这三个数字之和约 14.4 秒，与实测吻合。

## 3. 一次「改一个词」现在要经过哪六步

```
xr set <文件> <节点编号> <新值>
 1. 整份载入数据文件到内存 …………… 约 1 秒
 2. 检查是不是模板定义（护栏） ……… 几乎不花时间
 3. 改值；旧值抄一份进 @history …… 几乎不花时间
 4. 把整份文件重写回去 ……………… 约 4 秒   ← 待做：改成"只追加"（5.3 节）
 5. 把这个文件登记进本机目录 ……… 约 4.4 秒 ← 可挪到后台或批量
 6. 把这个文件重新登记进台账 ……… 约 5 秒   ← 待做：改成"只登记新增"（5.1、5.2 节）
```

第 6 步为什么贵：台账现在的规则是「给这个文件写一条新代号，它的旧条目一律作废」，所以每次改动都必须把**整个文件**的条目重算一遍、重写一遍。这是本文要解决的核心问题。

## 4. 已经做完的前提（不要再做一遍）

| 提交 | 做了什么 | 为什么 |
|---|---|---|
| `b0d903c` | `Store::save` 改成**原子写**（先写临时文件再改名） | 原来直接写目标文件，写到一半崩掉会损坏库 |
| `5dd164e` | 台账按「**编号 + 文件**」去重、日志优先 | 追加写之后同一编号会同时有新旧两条位置，不去重会返回两次、还可能读到旧内容 |
| `0458ee4` | 大文件的台账登记改到**后台进程** | 登记不该挡住用户的命令 |
| `6d3efef` | 台账日志改成**一次 write** | 原来每条记录一次系统调用，347 万节点约 700 万次 → 21.8 秒变 14.2 秒 |

**append-v1（数据层"只追加"）已经存在，并已接进一部分命令**：折叠读法在 `rust/core/src/tree.rs` 与 `tools/tree.py`（常量 `PROTOCOL_APPEND`）；校验放行在 `tools/validator.py`（声明了 `@protocol = append-v1` 的根下，重复编号不报 E002）；搜索/校验改成**流式扫描**（`rust/core/src/scan.rs`，提交 `44eedc1`）；`xr compact` **已经支持传单个文件**（`rust/cli/src/main.rs` 的 `cmd_compact`）。

**护栏（每次改完都要跑，不只跑新加的）**：

```bash
cargo test --manifest-path rust/Cargo.toml   # 15 个测试目标
python3 -m pytest tests/ -q                   # 77 条
cargo test --manifest-path app/Cargo.toml     # 桌面端 10 条
```

其中最关键的四条：`index_parity`（三种索引后端结果必须一致）、`index_faults`（索引坏了必须报错、不许静默给错答案）、`backend_parity`、`append_v1`，外加 `cli.rs::catalog_same_uuid_is_normal_union_and_only`（同一编号在两个文件里，`xr ws` 必须报「2 处」，防止"去重"退化成只按编号）。

## 5. 待做的三块

### 5.1 台账有效性规则（必须先做）

**① 遇到了什么问题。** 台账现在的规则是：给数据文件写一条「新代号」记录，这个文件的旧条目一律作废。这条规则对「整份重写」是对的（每个字节位置都可能变），但对「只追加」是灾难——追加只往文件末尾加几条记录，旧的字节位置其实一点没变，可规则会把它们全部作废，于是你只能整份重新登记。这就是第 6 步那 5 秒的来源。

**② 打算怎么解决。** 给台账里的「文件记录」加一个 `mode`（模式）字段，只有两种取值：

- `rewrite`（整份重写）：**代号 +1**，旧条目作废——就是现在的行为，保持不变；
- `append`（只追加）：**代号不动**，旧条目继续有效（旧记录的字节位置没挪动），只把变化的那几条作为「后写覆盖」追加进日志。

读数时（`Reader`）要遵守两条：结果按「**编号 + 文件**」去重、**日志优先**（提交 `5dd164e` 已做好）；文件记录是 `append` 时不要因为"代号没变"就跳过日志，是 `rewrite` 时才按老规则作废旧条目。

> 另一种做法是「每条条目都带代号、读数只认最新代号」。两者都能用，但**推荐 `mode` 方案**：条目不会变大（另一种会让定位条目 52→60、关系 68→76、反向 36→44 字节，台账体积从约 2.4 倍数据涨到约 2.8 倍），改动也更小。**选哪种要先跟维护者确认。**

**③ 三个最容易漏的要点。**

- **关系本（父→子）不需要墓碑**：息壤没有「移动节点」的命令，一个节点的父亲不会变，关系条目只会新增。
- **反向本（谁引用我）需要墓碑**：节点的引用值可以被改掉（`set` / `link`），旧的那条「旧目标 → 这个节点」必须显式作废，否则「谁引用我」会一直列出已经不指向我的节点。
- **物理删除要回退整份登记**：`rm` 是「置空」（记录还在），不需要墓碑；但 `history prune`（裁剪留痕）和 `tmpl rm`（删模板）会真正删掉记录，这类操作之后把这个文件**整份重新登记**一遍即可。

**④ 收益 / 成本 / 要不要人决定。** 收益：让「只登记新增的那几条」成为可能，是 5 秒降到毫秒级的前置条件。成本：时间上不增加步骤，空间上每个文件多 1 个字节，维护上多一条规则要记（必须配测试）。要你决定：选 `mode`（推荐，体积不变）还是「每条条目带代号」（体积 +15%）。

### 5.2 `append_tail`：只登记新增的那一段

**① 遇到了什么问题。** 第 6 步现在要把整个文件重扫一遍（347 万节点），即使你只改了一个节点。

**② 打算怎么解决。** 给台账加一个入口 `wsidx::append_tail(工作区, 文件, 从第几字节开始)`：

1. 只读文件「新增的那一段」（上次记录的文件长度 → 当前长度），逐条解码出新记录；
2. 每条新记录要知道它属于哪棵树（root）——**拿它的父编号去台账里查**（5.1 做完后旧条目仍然有效，所以查得到）；查不到就**放弃这一轮、回退成整份登记**（安全兜底）；
3. 给每条新记录写定位条目、关系条目；如果它的引用值变了，就给旧的反向条目写一条**墓碑**；
4. 最后写一条 `mode = append` 的文件记录（代号不动，指纹更新）。

**③ 收益 / 成本 / 要不要人决定。** 收益：第 6 步从约 5 秒降到与「改动条数」成正比（改一个词就是毫秒级）。成本：时间不增加，空间上日志增长更慢，维护上多一个入口要维护，且**必须有「查不到父节点就回退整份登记」的兜底**，否则会静默丢条目。要你决定：不需要。

### 5.3 单文件改成「只追加写」（数据层）

**① 遇到了什么问题。** 第 4 步现在每次都把整份文件重写一遍（187 MB 约 4 秒），哪怕只改了一个词；而且整份重写会顺手把 append-v1 累积的重复记录折叠掉，等于偷偷压了一次实。

**② 打算怎么解决。**

- 写路径改成：**只把这次改动到的节点追加到文件末尾**（现成的 `shard::append_changes` / `append_revision` 就干这个）；
- 追加之后在这个文件的根节点上补一条 `@protocol = append-v1` 声明（**幂等**：已经有了就不要再加，否则每次编辑都多一个节点，又是一个膨胀源）；
- **保留「整份重写」作为出口**：压实、另存为、导出仍然用它；
- ⚠️ **不要用「改前 / 改后两个完整 Store 做对比」**：在 347 万节点上克隆一份 Store 要 1–2 秒和约 1 GB 内存（按 1 百万节点约 258 MB 推算），会把省下的时间吃回去。正确做法是让写命令把「这次碰到的节点」交给追加函数——`set` / `rename` / `rm` / `link` / `revert` 都知道自己碰了谁，`fill` 碰的是几个目标，也不难收集。

**③ 收益 / 成本 / 要不要人决定。** 收益：第 4 步从约 4 秒降到毫秒级，文件不再每次被整份重写。成本：时间上无；空间上文件会缓慢累积旧记录，需要偶尔用 `xr compact <文件>` 瘦身；维护上多一条纪律——**「整份重写会折叠历史」这件事必须一直被记住**，否则某天一个不起眼的小改动会把追加攒下的历史悄悄合掉（不丢数据，但文件大小会突然变化）。要你决定：不需要；但它必须在 5.1、5.2 之后做，否则索引跟不上。

### 5.4 收尾两件

- **`xr compact <文件>` 的三条约束**：帮助与输出写明「只折叠这一个文件内部同一编号的多份记录，不跨文件、不合并编号」；打印「折叠了多少条 → 多少条、字节多少 → 多少」；支持 `--dry-run`，**不要加 `--yes`**（折叠不改变任何读得到的结果）。落盘必须走「临时文件 + 改名」。
- **桌面端编辑改调 CLI 写操作**：压实、留痕裁剪、索引维护一律复用现有命令，护栏（`--yes` / `force`）不要重写。桌面端现在是 Rust + egui（见 `app/`）。

## 6. 歧义检查（最容易互相误解的地方）

| 说法 | 可能被误解成 | 正确定义 / 怎么区分 |
|---|---|---|
| 「索引」 | 一个东西 | 有**三个**：本机目录（编号→文件）、台账（编号→文件+字节位置）、侧车（旧的每文件小账本）；说的时候要带限定词 |
| 「压实」 | 一件事 | 有**三件**：`xr compact <词库目录>`（折叠分片）、`xr compact <文件>`（折叠单文件内的追加记录）、`xr index compact`（把**台账**的日志合并回块）；三者互不相干 |
| 「重扫」vs「重写」 | 同一件事 | **重扫**＝读数据文件、重算台账条目（不动数据）；**重写**＝把数据文件整份写回去（动数据）。本文里两者永远分开 |
| 「登记」 | 写数据 | 指写**缓存**（目录 / 台账），不是写数据文件；登记慢 ≠ 数据慢 |
| 「代号 gen」vs「每条条目带代号」 | 同一件事 | 现在只有**文件记录**有代号；「每条条目带代号」是一种可选的新方案（5.1 节），会让条目变大 |
| 「整份载入」 | 读一点 | 指 `Store::load` 把整个文件读进内存；347 万节点约占 900 MB（按 1 百万节点 258 MB 推算） |
| 「两份 / 多处」 | 数据重复出错 | 同一个编号出现在**多个文件**里是**正常且重要**的（跨文件身份），不是冲突；`xr ws` 就该报「2 处」 |
| 「后台登记」 | 已经很快了 | 提交 `0458ee4` 把登记挪到后台，**但前台仍要 14 秒**；「54 → 44.3 秒」可能是**等后台跑完**才计时，口径要与第 2 节对齐后再下结论 |
| 「append-v1」 | 台账的事 | 是**数据文件**层的协议（允许只追加、读时同编号取最后一条）；台账怎么增量登记是另一件事（5.1 / 5.2） |
| 「留痕 / @history」 | 与 append 有关 | 是每次改动抄一份旧值用于回滚，**和 append-v1 无关**；它也是独立的膨胀源，用 `xr history prune` 裁剪 |
| 「墓碑」 | 删数据 | 只是**台账里的一条「此条目作废」记录**，不碰数据文件 |
| 「块 / 日志」 | 数据文件的一部分 | 都是**台账**内部的两种存储形态；数据文件里没有「块」 |
| 「尾部 / 新增的那一段」 | 文件末尾的任意字节 | 指**上次登记之后文件增长出来的那一段**（用长度对比算出来），只包含追加写进去的记录 |
| 「折叠读」 | 压实 | 是**读的时候**同编号取最后一条（append-v1 的读法）；压实是**写的时候**把它物理合并 |

## 7. 动手前需要先问清楚的四件事

1. **「54 s → 44.3 s」的口径**：是只计命令，还是等到后台登记结束？建议按第 8 节重测一次再定目标。
2. **继续在 `codex/app-editor` 分支，还是新开分支**（该分支目前签出在 worktree `.worktrees/app-editor`）。
3. **`index::compact_file` 的命名**：它折叠的是**数据文件**、不是索引，建议改名（例如 `tree::fold_file`）。
4. **`@protocol = append-v1` 是否已经在 CLI 写路径里自动补**？如果没有，那属于 5.3 的一部分，别以为已经有了。

## 8. 可直接照抄的命令

```bash
# 0) 准备：拿那份 187 MB / 347 万节点的测试文件
cp .exp/format-compare/data/S4_生物.xirang /tmp/s4/big.xirang
XR=rust/target/release/xr              # 记得 cargo build --release
export XIRANG_CATALOG=/tmp/s4/cat.idx
export XIRANG_INDEX_MAINTENANCE=off    # 测性能时关掉后台整理，避免干扰

# 1) 建一次台账（一次性，约 4 秒）
$XR index rebuild /tmp/s4/big.xirang

# 2) 测「改一个词」（这就是要优化的动作）
ID=$($XR cat /tmp/s4/big.xirang --head 1 --ids | sed -n 's/.*<\([0-9a-f-]\{36\}\)>.*/\1/p')
time $XR set /tmp/s4/big.xirang "$ID" 测试值

# 3) 拆解：关掉本机目录再看一次（差值就是目录登记的开销）
time env XIRANG_INDEX=off $XR set /tmp/s4/big.xirang "$ID" 测试值2

# 4) 看台账状态与一致性
$XR index status
$XR index check --sample 50

# 5) 护栏（改完必跑）
cargo test --manifest-path rust/Cargo.toml
cargo test --manifest-path app/Cargo.toml
python3 -m pytest tests/ -q
```

## 9. 一页速查

- **目标**：187 MB / 347 万节点，「改一个词」从 **14.2 秒**（修复前 21.8 秒）降到 **≤50 毫秒**。
- **剩余三块，顺序不能跳**：台账有效性规则（`mode`）→ `append_tail` → 单文件追加写。
- **各自省多少**：台账重扫约 5 秒 → 毫秒；整份重写约 4 秒 → 毫秒；本机目录约 4.4 秒 → 可挪后台。
- **最容易做错的**：去重键必须是「编号 + 文件」（不能只按编号）；反向本要墓碑、关系本不要；物理删除后回退整份登记；不要用整份 Store 克隆做 diff；`@protocol` 要幂等。
- **文档关系**：数据与协议看 `spec/协议.md`；CLI 现有能力与性能看 `CLI现状.md`；这条路线与验收看 `开发计划.md`。本文件是**第 2 步唯一的施工说明**。
