# AGENTS.md

息壤（XiRang）：一种极小「节点语言」的规范仓库。核心是纯文档规范；`tools/` 有 Python 参考实现（编解码器 + 校验器）。

## 关键事实（别踩坑）

- **代码与测试**：`rust/`（`cargo`，交付实现 `xr`/`xr-mcp`）+ `tools/`（Python 参考实现，语义与 `rust/` 对齐）+ `app/`（Rust + egui 桌面）。跑测试：`python3 -m pytest tests/`、`cargo test --manifest-path rust/Cargo.toml`、`cargo test --manifest-path app/Cargo.toml`；正式行为以 `rust/` + `spec/` 为准。
- 全部文档为中文，Markdown 文件名无空格、无英文（如 `errors/错误列表.md`）。
- 版本采用「解耦 + 依赖声明」：内核（README 的「节点 / 节点值」部分）是唯一核心版本，外围各自独立版本并标注「依赖内核 vX.Y」，见 `spec/版本规范.md`。
- 仓库远程为 GitHub：`https://github.com/outnever/XiRang`（唯一公开地址）。
- 过时文件（旧 README、内核规范、模板与用例、notes/ 等）已移出本仓库，不再活跃编辑。

## 术语红线（写文档/改文档前必读）

词汇表已随内部材料移出本仓库。写作时**统一用固定词汇，避免同义词混用**。几组最易混、最容易写错的对：

| 对 | 区别 |
|----|------|
| 节点树正文 vs 辅助节点 | 节点树正文 = 无前缀，参与当前视图；辅助节点 = `@` 前缀，默认不参与（`@format`/`@address`/`@fingerprint`/`@history`/`@note`/`@source`/`@protocol` 等） |
| 内核 vs 协议 | 内核 = 节点是什么（四属性 + 节点值类型标记，**只增不改、永不复用**）；协议 = 树怎么组织 / 怎么存（可扩展） |

## 架构约束（不要违反）

- **内核只增不改**：节点固定四字段（节点编号 / 父节点 / 节点名 / 节点值）；节点值的大类标记固定 7 种（空 / 整数 / 浮点数 / 布尔 / 文本 / 引用 / 二进制块）。不要给节点增删字段或大类（旧标记永不复用）。这类扩展一律进「协议」。
- **新增一种树 → 写协议**，不是改内核：用基础约定描述结构 + 填 `spec/协议.md` 的「协议必须项」，登记到各自项目。
- **错误码只追加、永不重排/复用**：格式 `X<三位>`，按层分域——节点层 `E` 结构 / `R` 引用（已生效），模板层 `V` 版本 / `O` 顺序（预留），实现层 `F` 文件 / `C` 转换（已生效）/ `W` 写入器（已生效，含 `F` 索引码 `F005+` 与 `W` 分片 / 目录码）；清单在 `errors/错误列表.md`。
- **版本解耦**：只在内核（四属性 / 值编码 / 文件外壳）改动时才升内核版本；外围（协议 / 约定 / 实现 / 错误码）各自独立版本 + 声明「依赖内核 vX.Y」，不把依赖拼进版本号。见 `spec/版本规范.md`。

## README 写作原则

README 是公开发布的门面，只写**使用者 / 大模型需要知道的**：核心设计（节点四字段、7 大类、字节布局、文件头）与英文解析参考。**维护者内部约束一律不进 README**，留给 `spec/` 文档：
- 兼容性承诺（如「标记永不复用」）、升版规则、错误码设计 → `spec/版本规范.md`、`errors/错误列表.md`。
- 内部迭代历史、模型测试记录、审查报告 → 放在仓库外的内部存档，不要写进本仓库。

## 阅读顺序

README（内核，稳定核心）→ `spec/协议.md`（协议层官方建议）→ `spec/版本规范.md` → `errors/错误列表.md`。
