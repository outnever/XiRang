# 验收测试清单（用户手动测试）

> **这是什么。** 一份给你（使用者）手动验收用的清单：每行 =「敲一条命令（或点一个操作）→ 应该看到什么」。看到符合预期，就在前面打勾。
> **怎么用。** 一节一节往下做即可，不必一次做完；每项左边的方框 `[ ]` 是待办，测过之后勾成 `[x]`。
> **进度。** A（命令行）、F（命令行查询 / 模板 / MCP）和 B / C（桌面版浏览与编辑，2026-09 重做版）现在就能测；D（多文件 + 图）还没接进界面，等 d4。
> 桌面版启动：`cargo run --manifest-path app/Cargo.toml`（或直接跑 `target/debug/xirang-app`）。

## A. CLI（M1 已完成，现在即可测）

用仓库里的 `spec/息壤文档.xirang` 测试（命令用 Rust 单二进制 `xr`；早期 Python 版 `python3 -m tools.cli` 语义一致，也可用）：

- [ ] `xr info spec/息壤文档.xirang` → 显示格式版本 1、头文本首行、节点数、根节点数 1
- [ ] `xr tree spec/息壤文档.xirang` → 缩进树层级正确，`名字 = 值`、`→ 目标`、`[blob N 字节]` 显示正确
- [ ] `xr tree spec/息壤文档.xirang --node <某节点UUID>` → 只打印该子树
- [ ] `xr tree spec/息壤文档.xirang --skip-aux` → 不显示任何 `@` 开头节点
- [ ] `xr tree spec/息壤文档.xirang --ids` → 每个节点后带编号
- [ ] `xr tree spec/息壤文档.xirang --head 5` → 只打印 5 个节点，提示「只打印了前 5 个节点，共 108 个」出现在 stderr
- [ ] `xr tree spec/息壤文档.xirang --depth 1` → 只打印根节点；stderr 提示「只展开到第 1 层」
- [ ] `xr cat spec/息壤文档.xirang | head` → 一行一个节点（无缩进、无 `├─`），顺序是文件里的存放顺序
- [ ] `xr cat <一个百万级文件>` → 被护栏拦住并提示用 `--head`；加 `--force` 才会真打
- [ ] 在终端里 `xr tree spec/息壤文档.xirang` → 自动进分页器（q 退出）；加 `--no-pager` 则直接打印；`xr tree … | head` 行为与以前一致
- [ ] `xr validate spec/息壤文档.xirang` → 输出「校验通过：0 错误」
- [ ] `xr rename spec/息壤文档.xirang <某节点UUID> 测试改名` → 打印「已改名」，`xr tree --ids` 里名字变了、编号没变
- [ ] 承接上一条：`xr validate spec/息壤文档.xirang` → 仍是「0 错误」；`xr rename … ""`（空名字）→ 被拒绝、退出码非 0
- [ ] `xr info README.md` → 报 F001 魔数非法、退出码非 0

## B. 桌面版 · 浏览（重做版已可测）

- [ ] 打开单个 `.xirang`（工具栏「打开…」或 ⌘O）→ 树视图渲染，根节点在顶层
- [ ] 未展开的节点显示 `▸ N`（N = 孩子数量），叶子显示 `·`；点一下展开、再点收起
- [ ] 7 大类值显示正确：空（无值）、整数、浮点、布尔 true/false、文本、引用显示为「→ 目标名」、二进制块显示「[blob N 字节]」
- [ ] 布局切换：「横向缩进」是 CLI 风（`├─` / `└─` 引导线），「纵向分层」缩进更宽
- [ ] 辅助节点开关 → 关掉后 `@` 开头的节点整体消失
- [ ] 底部状态栏显示节点数 / 修订数 / 缓存命中与直读次数

## C. 桌面版 · 编辑（重做版已可测）

- [ ] 选中节点 → 右侧面板显示名字与值；「保存改值」后立刻生效（文件里追加了一条记录）
- [ ] 「保存改名」→ 名字变了、编号没变
- [ ] 「＋ 新增子节点」→ 新节点出现在该节点下
- [ ] 「删除（置空）」→ 变成灰色空槽位，编号还在
- [ ] 撤销 / 重做（工具栏按钮）→ 一步步回退 / 前进，每一步都落盘
- [ ] 「校验」→ 底部显示「校验通过：0 错误」或列出 E/R 错误
- [ ] 编辑过的文件再用命令行读：`xr tree <文件>` 正常、`xr validate <文件>` 通过

## D. 桌面版 · 引用图（Obsidian 复刻版，已可测）

**并排比对**（关键验收方式）：拿一份 Obsidian 的同一张图谱（截图或它导出的页面），
与本程序打开同一个有引用的文件（如 `spec/模板.xirang`，22 条边），两边对着看。

- [ ] 布局形态一致：同一张图收敛后的整体形状、疏密、边长观感相同
- [ ] 缩放手感一致：滚轮每 120 单位 1.5 倍、以光标为锚点、缩小回视图中心、缩放是"滑过去"的（每帧 15%）
- [ ] 文字淡入阈值一致：把「文字淡出」滑杆从 −3 拉到 +3，两边名字出现/消失的缩放点相同
- [ ] 悬停：命中节点高亮环、邻居保持、其余压暗到 0.2；标签下移 15px 并放 1:1 字号
- [ ] 拖节点：钉住 + 邻居让位（alphaTarget 0.3）；松手不钉住、重新参与布局
- [ ] 拖空白：平移 + 松手惯性（松手前 100ms 内才带速度）
- [ ] 箭头开关：放大到一定程度才淡入（`2(scale−0.3)`）；双向边只画一支
- [ ] 参数滑杆：中心引力 / 排斥力 / 连线拉力 / 连线长度 拖动后布局变化趋势与参考一致
- [ ] 静止后 CPU 归零（静止 60 帧停止重绘）
- [ ] 「孤立节点 / 附件 / 标签 / 隐藏未解析 / 颜色分组 / 局部图谱」六个开关各自生效
- [ ] 「重置视图」回到自适配缩放 + 原点居中
- [ ] 图里点选 → 右侧检查器跟着切换；单击节点 → 进入「聚焦此子树」的局部图谱

**数值对拍**（自动）：`cargo test --manifest-path app/Cargo.toml --test golden_physics` ——
与参考实现逐位比对，偏差应 ≤ 1e-6 世界单位（实测 1e-13 量级）。

用一个带引用的词库文件（比如 `测试/下.xirang` 或你自己造的引用数据）：

- [ ] 工具栏切到「引用图」→ 引用关系画成一张网，节点大小随连接数变化
- [ ] 缩小到一定程度 → 节点名消失；鼠标悬停某个节点 → 显示「节点名 + 所在文件」
- [ ] 放大 → 节点名重新淡入出现
- [ ] 悬停一个节点 → 它的邻居高亮、其余变淡
- [ ] 拖动一个节点 → 整张图跟着动（松手后慢慢静止）；拖空白处平移、滚轮缩放
- [ ] 「图设置」→ 拖「图谱向心力 / 节点间的排斥力 / 相连节点间的吸引力 / 连线长度」→ 布局实时变化；「文字淡入阈值」拉到负值 → 缩小也能看到名字
- [ ] 「显示孤立节点」勾上 → 没连接的节点也出现在图里
- [ ] 辅助节点开关（工具栏）→ 关掉后图里的 `@` 节点消失
- [ ] 选中一个节点 → 右侧「聚焦此子树」→ 这棵子树按树形排开，图里其余节点淡出成背景；再点「取消聚焦」回到力导向布局
- [ ] 「重置为默认」→ 回到 Obsidian 那套默认参数（0.1 / 10 / 1 / 250）

## D2. 桌面版 · 多文件 / 搜索 / 历史 / 导出（已可测）

- [ ] 一次选两个文件打开（工具栏「打开…」可多选）→ 顶部出现文件下拉，切换即换文件
- [ ] 两个文件里有互相引用的节点 → 切到「引用图」→ 跨文件的引用边也连上了
- [ ] 搜索：输入关键字 → 选「名字 / 值 / 类型」→ 点「搜索」→ 底部列出命中，点一个跳回树并自动展开到它
- [ ] 大文件里搜索 → 界面不卡（后台线程扫描），可点「取消」
- [ ] 校验：点「校验」→ 大文件也不卡（流式扫描，不整份载入），错误列在底部
- [ ] 选中一个改动过的节点 → 底部出现 `@history` 快照，点一下 → 回滚到那个快照（会追加一条记录）
- [ ] 「导出 ▾」→ 选 json / xml / yaml / md → 选保存位置 → 文件生成且内容正确
- [ ] 选中一个 blob 节点 → 右侧显示字节数、十六进制头、文本预览；「导出为文件」能把二进制写回去
- [ ] 「合并」→ 记录数变少、状态栏提示折叠掉多少条；文件仍能正常打开
- [ ] 关掉程序再打开 → 自动回到上次的文件，布局与展开态被记住（视图态存在用户配置，不在 `.xirang` 里）
- [ ] 侧栏「最近打开」列出用过的文件，点一下即可打开
- [ ] 打包：跑 `scripts/make_macos_app.sh` → 生成 `dist/XiRang.app`，双击里面的可执行文件能启动

## D3. 桌面版 · 外观 / 语言 / 导出范围 / 图片（已可测）

- [ ] 工具栏点「中文 / EN」→ 界面在中文与英文之间切换，重开程序仍记得
- [ ] 工具栏点「配色」→ 试「暗色 / 亮色」预设；再拖颜色选择器改背景 / 强调色 / 辅助节点色 / 边框 → 界面立刻变；重开程序仍记得；「重置为默认」回到暗色
- [ ] 树视图里 `@` 辅助节点用「辅助节点色」显示（和图里一致）
- [ ] 导入一个图片文件为 blob（或用已有数据）→ 选中该节点 → 右侧直接显示图片
- [ ] 换到别的节点 → 图片消失（纹理被释放，内存不堆积）
- [ ] 导出窗口里选「当前视图」→ 导出的文件只包含当前展开的那些节点（折叠着的部分不含）
- [ ] 选「选中子树」→ 导出的文件以选中节点为根，且能正常转成 Markdown（不会导出成空）
- [ ] 选「完整折叠视图」→ 导出整份（同编号只留最后一条）

## E. 桌面版 · 大文件与性能（重做版已可测）

用那份 179 MB / 347 万节点的文件（本地 `.exp/format-compare/data/S4_生物.xirang`）：

- [ ] 打开大文件 → 界面立刻出来（不转圈、不整份载入）
- [ ] 展开 / 收起大节点（几十万孩子的）→ 只加载可见的那部分，滚动依然顺
- [ ] 在大文件上改一个词 → 立刻生效、文件只多了几十字节
- [ ] 选中节点后底部状态栏的「直读 / 命中」计数符合预期（第一次直读，再点就是命中）
- [ ] 暗 / 亮主题切换 → 即时生效
- [ ] 打开中小文件 → 树渲染与编辑手感正常
- [ ] 打开大文件后看底部：常驻内存约 160 MB（不是 1 GB），节点缓存有上限
- [ ] 点「释放缓存」→ 缓存归零；继续滚动会自动按索引读回来
- [ ] 放着不动约 6 秒 → 状态栏提示缓存被压缩（空闲自动释放）
- [ ] 切到「引用图」再切回来 → 图数据与行列表都被释放（状态栏的图节点数归零）
- [ ] 点「关闭」→ 缓存、行列表、图数据一起释放

> 合并按钮、导出、双击文件关联仍在 d5 待做。

## F. CLI 查询 / 模板 / MCP（M7 解锁，现在可测）

用 `rust/target/debug/xr` 测试（构建：`cargo build -p xirang-cli`）：

- [ ] `xr match <file> --root 词条 --where 词形=灯 --json` → 命中且输出结构化数组
- [ ] `xr match <file> --shape-of <某词条节点ID>` → 命中拓扑相同的树（发现），`--where` 进一步筛值
- [ ] `xr new <file> nil 根`（文件不存在时自动新建）；在非辅助父节点下 `new` → 一行「改形状码」软提示，`--yes` 跳过
- [ ] `xr copy <file> <节点> nil --blank --no-history` → 复制出只有结构、值空、无 `@history` 的骨架，内部引用重指、外部引用保持
- [ ] `xr fill <file> <root> 词形=灯 词义/01/释义=照明器具` → 按名字/路径赋值；`--no-history` 不记 `@history`
- [ ] `xr tmpl add <file> 词条 --from-json <sample>` → 建一棵标 `@模板`(空) 的模板定义；`xr tmpl list` 显示「词条 <uuid>（N 实例）」
- [ ] `xr tmpl rm <file> 词条` 无 `--yes` → 阻止并提示；`--yes` 连同其所有实例一并删除
- [ ] `xr import <file> --template 词条 <data.json>` → 每条记录一棵实例（根挂 `@实例`+`@模板`→模板，按名字填值、缺失留空）；`xr instances 词条` 列出
- [ ] `xr import <file> --template 词性 <data.json> --under <某词条节点>` → 实例嵌套挂到父下（词性在词义/01 下），`instances 词性` 仍能找到
- [ ] `xr diff <a.xirang> <b.xirang>` → 显示增/删/改（含名字+值前后）；`--json` 结构化
- [ ] `xr-mcp`：`tools/list` → 返回 10 个按领域分组的工具（`context`/`file_info`/`file_validate`/`file_diff`/`tree`/`query`/`node`/`template`/`convert`/`blob`），每个都带 `inputSchema` 与说明
- [ ] `xr-mcp --root <目录>`：`context` 回报该目录；传 `../x.xirang` 或目录外的绝对路径 → `path_denied`；传分片词库目录 → `unsupported`
- [ ] MCP 与 CLI 一致：同一串操作（new/set/rename/rm/link/copy/fill）走 `xr` 与走 `xr-mcp` → `xr tree --skip-aux` 结果一致
- [ ] MCP 护栏：改模板定义节点不带 `force` → `guarded`；`template` 的 `remove` 不带 `force` → `guarded`，带 `force` → 连同实例删掉
- [ ] MCP 覆盖保护：`convert` 的 `import` 覆盖非空文件、`blob` 的 `export` 覆盖已有文件 → 不带 `force` 被拦，带 `force` 才生效
- [ ] `xr-mcp` 不碰本机目录：纯 MCP 会话后 `xr catalog list` 仍是 0 个文件

## G. 工作区统一索引与对比基准

- [ ] 默认走工作区台账：`xr new/set` 后数据目录**不**再出现 `.xirang.idx`，而是 `.xirang-index/`（`index.manifest` + `loc/rel/rev` 的块与日志）
- [ ] `xr index status` → 显示文件数 / 代数 / 块数 / 主干与日志体积；`xr index check` → 抽样节点都能定位并读出
- [ ] `xr index compact` → 日志被合并回块（代数 +1，日志变小）；压实后 `xr ws`、`xr instances` 结果不变
- [ ] 跨文件解析：A 文件里的节点被 B 文件引用 → `xr ws <编号> A B` 能同时列出「目标在 A」与「来源在 B」
- [ ] 回退：删掉 `.xirang-index/` 后 `xr ws` 仍能给出结果（整份载入回退）；`xr index rebuild` 后恢复走索引
- [ ] 可切换：`XIRANG_INDEX_MODE=sidecar` 时行为回到旧的每文件 `.idx`，两种模式结果一致
- [x] 对比基准：本轮由一次性基准工具跑出（工具与原始数据不随仓库发布），结论见「开发计划 → 测试结果摘要」
- [x] 故障演练（本轮一次性验证）：日志尾部半条记录不影响读取；块文件被删 → 报 F012 且 `xr ws` 回退整份载入并提示；两个进程同时压实 → 后者被锁拒绝；陈旧锁自动回收
- [x] 自动整理：写命令跑完后日志被后台压实；`XIRANG_INDEX_MAINTENANCE=off` 时不启动
- [ ] 入口开销：100 万节点 / 445 个文件时 `xr ws` ≤ 15 ms（实测 7.6 ms）
- [ ] 索引管理（常驻护栏 `rust/cli/tests/index_guard.rs`）：`xr index files --stale` 能标出被外部改过的文件，`xr index update` 之后恢复同步
- [ ] `xr index drop` / `forget` 不带 `--yes` → 退出码 2 且不动手；带 `--dry-run` 只预演；`drop --yes` 只删 `.xirang-index/`，数据文件字节不变
- [ ] `xr index status --json` / `files --json` 能被 `python3 -c 'json.load(sys.stdin)'` 解析；`xr index path` 打印的目录真实存在
- [ ] `xr index unlock` 报出锁里的 pid 并删除锁文件；`xr index forget <文件> --yes` 后该文件的编号查不到、数据文件仍可读
- [x] 留痕裁剪：`xr history prune <file> <id> --keep 5` 无 `--yes` → 退出码 2 并报出「会丢掉几条」；`--dry-run` 文件不变；`--yes` 之后只留最近 5 条、文件变小、`xr revert` 仍能回滚到保留的那条
- [x] 同一命令走 MCP（`node` 的 `prune_history`）：不带 `force` → `guarded`，带 `force` → 与 CLI 结果一致
- [x] Python 参考实现同步：`tools/tree.py` 的 `prune_history` 与 Rust 语义一致，且 Python 裁过的文件交给 Rust 校验通过

---

## H. 只追加写与台账增量登记（第 2 步，2026-09）

常驻护栏：`cargo test --test index_append_tail`（5 条）/ `--test append_write_min`（2 条）/ `--test index_faults`（7 条）；
Python 侧 `python3 -m pytest tests/test_append_v1.py`。

- [ ] **改一个词只追加**：`xr set` 之后文件只长大几百字节以内（整份重写会大得多）；同一个根下 `@protocol = append-v1` **只加一次**（连改 5 次仍只有一条）
- [ ] 值读得回来；`xr validate` 仍是「0 错误」——重复编号靠协议声明放过，**不报 E002**
- [ ] `xr index status` 改完之后**没有**「指纹不符」；`xr index check --sample 50` 报「一致」
- [ ] **引用改掉要写墓碑**：把某节点的引用从 A 改成 B → `xr refs A` 不再列出它、`xr refs B` 列出它（旧边显式作废）
- [ ] **折叠（压实）**：`xr compact <文件>` 打出「记录 N → M、字节 X → Y」；`--dry-run` 不动文件；没有重复记录时明说「一个字节都没改」；**不需要** `--yes`
- [ ] 折叠之后再 `xr index status`：没有「指纹不符」（折叠会整份重写，台账要跟上）；值仍读得回来
- [ ] **物理删除仍走整份重写**：`xr history prune <文件> <编号> --keep 2 --yes` 之后文件变小，且台账没有「指纹不符」
- [ ] **大文件**（179 MB / 347 万节点）：`xr set` 只追加（台账日志每次约 10 KB，之前是整份重扫、300+ MB）；默认设置下约 3 秒，其中约 2.9 秒是「整份载入」
- [ ] **缓存转后台**：默认设置下改大文件会打一行「已在后台进行」（索引登记 / 本机目录登记），命令不等它；`XIRANG_INDEX_MAINTENANCE=off` 时改成同步做
- [ ] **旧格式台账**：把 `.xirang-index/index.manifest` 的格式版本字节改成 1 → `xr index status` 报 F013 并提示重建；`xr cat/tree` 仍能读（回退整份载入并提示）；`xr index rebuild` 后恢复
- [ ] **同一编号多文件仍是正常现象**：`xr ws <编号> A B` 仍报「2 处」（护栏 `cli.rs::catalog_same_uuid_is_normal_union_and_only`）
- [ ] **单节点写按编号直读**（不整份载入）：文件建过台账之后，`xr set / rename / rm / link` 都走这条路（护栏 `cli/tests/append_write_min.rs::set_uses_the_direct_path_once_the_ledger_is_ready`）；347 万节点的文件上改一个词约 10 毫秒
- [ ] **退回整份载入也是对的**：把 `.xirang-index/` 删掉再改 → 仍然改得动、写出来的结果一样，并且顺手把台账建回来（再改一次又走直读）
- [ ] **桌面端与 CLI 同一套落盘规则**（护栏 `app/tests/edit_shared.rs`）：桌面端改完，`xr find` 读得到；重开编辑器再改，`@protocol = append-v1` 仍只有一条；撤销后值回到原样
- [ ] **批量提交**（护栏 `cli/tests/batch_min.rs`、`core/tests/edit_path.rs` 的三条批量用例、`cli/tests/mcp.rs::node_batch_over_mcp`）：
      `xr batch <文件> <清单>` 一个进程改一批；`--dry-run` 只校验不写文件；清单里有一行坏（编号不存在）→ **整批不动**、退出码 2、报错带条号；
      同一编号在一批里改多次 → 以最后一次为准、只多一条记录；跑完 `xr validate` 0 错误、`xr index status` 没有「指纹不符」；`-` 可从 stdin 读清单；`--json` 输出可解析
- [ ] **40 万条量级的迁移**：`--no-history` 一个进程跑完（实测 105 秒 / 187 MB 文件 / 文件只增长约 27 MB），跑完补一次 `xr index compact`（4.4 秒）把台账日志并回块
- [ ] **没登记的文件直接跑**（护栏 `core/tests/edit_path.rs::batch_auto_registers_an_unregistered_file`、`cli/tests/batch_min.rs::batch_on_an_unregistered_copy_works`）：把词库复制到干净目录（天然没登记）→ `xr batch` 直接成功，不该出现「这个文件还没登记过」把流程卡住
- [ ] **引用目标不存在 → 整批拒绝并报条号**（护栏 `batch_refuses_missing_reference_target`）：`link` 到幽灵编号 → 退出码 2、文件哈希不变；加 `--allow-missing-target` 才放行
- [ ] **`rm_subtree` 连子树一起清空**（护栏 `batch_rm_subtree_empties_every_node_in_the_subtree`）：父 + 两个孩子都置空、子树之外不动；带留痕时每个被清空的节点都留下 `@history`（可 `xr revert` 还原）
- [ ] **一次提交跨分片**（护栏 `cli/tests/batch_min.rs::batch_over_a_shard_directory`）：目标是词库目录时，清单里的编号自动分派到各分片；预演先都校验，输出逐文件报账
- [ ] **日志大不再拖慢**：日志涨到几十 MB 之后，同样 2 万条改动的预演应当仍是秒级（实测 28 MB 日志 / 2 万条 = 7.7 秒，干净工作区 5.1 秒）；日志超过 64 MB 时命令会提示先跑 `xr index compact`

---

## 已知限制 / 备注

- CLI 已是 Rust 单二进制 `xr`（`cargo run -p xirang-cli -- <cmd>` 或 `rust/target/release/xr`）；Python `tools/cli.py` 作为早期 MVP 保留。
- `.xirang` 含随机 UUID，每次重建字节会变（内容语义不变），测试时以内容为准，不要比对字节。
- 追加写会让文件缓慢变大（同编号攒多份记录），用 `xr compact <文件>` 收；裁定是「不自动折叠、只在需要时提示」。
- 单节点写已不再整份载入（CLI 约 10 ms / 桌面端 11 ms，共用 `rust/core/src/edit.rs`）；整份载入只剩导出 / 校验 / 需要整棵树的命令，以及「台账不可用时自动退回」这条兜底路径。
