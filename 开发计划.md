# 息壤阅读编辑工具 · 开发计划

> 目标：桌面版 + CLI 版的息壤（`.xirang`）文件阅读 / 编辑工具，支持 macOS / Windows / Linux。
> **怎么读。** 这是一份开发进度表：`[x]` = 已做完，`[~]` = 做了一部分，`[ ]` = 还没做。技术选型见 §0。
> 状态：规划中。技术决策已确认（见 §0）。本文件用于开发跟踪。
> 说明：本工具可能后续拆分为独立仓库；当前先在本仓库跟踪计划。

---

## 0. 技术决策（已确认）

- **技术栈**：Rust 核心库（解析 / 校验 / 编辑逻辑，一份代码）+ **Tauri 桌面** + **Rust CLI**，三端共享同一核心。CLI 的 MVP 可先用 Python 直接复用现有 `tools/` 快速验证，再移植到 Rust。**已落地**：Rust 核心库 + `xr` / `xr-mcp` 就是交付实现，Python `tools/` 退为参考实现。
- **桌面端技术栈（2026-09 重做）**：**Rust + egui（eframe，glow 后端）**，与 `xirang-core` 同进程调用，不再用 Tauri / WebView。依赖只有 `xirang-core` + `eframe` + `rfd`。画布是一块，树与网络共存（见 §10）。
- **读与写（2026-09 重做）**：读走**懒加载**（侧车索引按偏移直读，不整份载入），写走**单文件追加**（`append-v1` 协议：同编号多记录 = 修订，后写覆盖）+ 手动合并。实测 179 MB / 347 万节点：冷开 0 ms、取一个节点 16 µs、改一个词 11 ms（改造前 20.5 秒）。
- **跨文件引用持久化**：**改由 `catalog-v1` 本机全局目录承担**（实现层缓存，见 §5 与 `spec/协议.md`）：跨库定位靠「UUID → 文件」，不往 `.xirang` 里写路径；原先设想的辅助节点 `@address` / `@source` 方案**不再采用**。
- **折叠态 / 视图态存储**：默认存内存（桌面版现状如此）；可选 sidecar `<文件>.view.json` 持久化**未做**。**不写进 `.xirang`**（视图 ≠ 数据）。

---

## 1. 架构分层

```
┌─────────────────────────────────────────────┐
│  桌面 GUI（Rust + egui：单一画布，树 / 网络）  │
│  　· 树视图 / 图视图 / 编辑面板 / 搜索 / 帮助   │
├─────────────────────────────────────────────┤
│  CLI（xr 命令：Rust 二进制）                  │
├─────────────────────────────────────────────┤
│  核心库 core（Rust）：                        │
│  　codec（编解码）· validator（校验 E/R）      │
│  　tree（树/图遍历、寻址 + 增删改查、留痕）    │
│  　convert（格式转换）· query（按结构/名/值查）│
│  　index（sidecar 索引）· shard（分片词库）    │
│  　catalog（本机目录）· workspace（多文件工作区）│
├─────────────────────────────────────────────┤
│  文件系统 / 导入导出（JSON·XML·YAML·Markdown） │
└─────────────────────────────────────────────┘
```

核心库模块就是上面这些（`rust/core/src/`：`codec` / `validator` / `tree` / `convert` / `query` / `index` / `shard` / `catalog` / `workspace`）。语义与 `tools/` 对齐：节点四字段、7 大类、辅助节点约定、改/删留痕（`@history` + `@replaced`）、错误码 E/R/F/C/W。撤销 / 重做是**桌面版前端**的会话层（`app/dist/main.js`），不占核心库模块。

---

## 2. 功能清单（分阶段）

### P0 — MVP：能读、能看、CLI 骨架

- [x] CLI：`xr open <file>` 载入并 `info`（节点数 / 格式版本 / 头文本摘要）
- [x] CLI：`xr tree [<node-id>]` 打印缩进树
- [x] CLI：`xr validate` 跑 E/R 校验并输出错误码
- [x] 桌面：打开单个 `.xirang`，树视图可折叠
- [x] 桌面：7 大类值正确显示（空/整数/浮点/布尔/文本/引用/二进制块）
- [x] 桌面：辅助节点显 / 隐开关
- [x] 渲染：朴素实现（不做大规模优化）

### P1 — 编辑 + 单文件完整闭环

- [x] 新增节点（自动挂 `@created` 时间）
- [x] 改值（旧值进 `@history` + `@replaced`）
- [x] 删（置空除编号外字段 + `@history` 留痕，槽位保留）
- [x] 查（`xr find` 按名字 / 文本值搜索）
- [x] 折叠节点 + 折叠态标识（桌面版）
- [x] 导入 / 导出：JSON / XML / YAML 无损往返；Markdown 有损导出
- [x] 无损导出（含编号 / 父编号 / 值类型）与有损导出（名字当键）两种
- [x] 帮助文档（CLI `--help` + 桌面帮助弹窗）
- [x] 撤销 / 重做（桌面撤销/重做完整闭环）

### P2 — 多文件 + 图 + 引用

- [x] 多文件同窗口浏览（核心 Workspace + CLI `ws` + 桌面多文件分组树 + 跨文件图）
- [x] 全局索引 `UUID → (文件, 节点)`，同时打开时跨文件引用自动连线（内存态 `workspace`；落盘的跨库目录见 P5 的 `catalog`）
- [x] 图视图：引用边 + 反向边（「谁引用了我」）（桌面图视图 + CLI `refs`）
- [x] 创建 / 编辑 / 删除引用（连边）（CLI `link` / `set`）
- [x] blob 导入 / 导出 / 预览（CLI `blob-import`/`blob-export`/`blob-info`）
- [x] `@history` 时间线 + 一键回滚（CLI `history` / `revert`；「新旧值 diff」未做）
- [x] 实时校验 lint（桌面打开时显示 E/R 错误）

### P3 — 性能 + 渲染 + 打磨

- [ ] 虚拟化渲染（大量节点同屏）
- [~] 按需解析（流式迭代器 `NodeIter` 已实现；sidecar 索引已含根块 / 归属块 / 反向块 / **父子块**，能按父边直接取孩子，跨文件按树懒加载；仍未做的是 **mmap + 视口虚拟化**）
- [~] 渲染风格 / 主题（CSS 变量切换，**暗 / 亮两套已实现**；「自定义」未做）
- [x] 分屏 / 多视图（树 / 图 / 分屏三态切换）
- [~] 导出范围扩展（`export --subtree <id>` 已实现；「当前视图含折叠态」未做）
- [ ] 文件关联（双击 `.xirang` 打开）+ 最近文件
- [x] 自动保存 + 只读模式（每次编辑即时落盘；只读开关禁用编辑）
- [x] i18n（中 / 英界面切换）

### P4 — 查询层 / 模板 / MCP（后补）

- [x] 加载时物化的**形状码**索引（拓扑规范编码，不区分顺序、跳过辅助节点、interning 无碰撞；不改盘上格式）
- [x] **名字索引** + `xr match`（按根名 `--root` / 形状码 `--shape-of` / 模板实例 `--template` 三选一 + `--where` 值约束；`--json`/`--tree`）
- [x] **`@模板` / `@实例`** 注释式约定：模板=根挂 `@模板`(空)；实例=根挂 `@实例`(空)+`@模板`(引用→模板)；**无容器、无例外**，实例可自由挂任意父下
- [x] **编辑规则**：找最近「有标注的根」——`@模板`(空)根→受保护，`@实例`根/其它→可编辑
- [x] 模板从**样例 JSON** 定结构（`xr tmpl add --from-json`），`--under` 让实例自由挂载
- [x] **批量实例化**：`xr import --template <name> <data.json> [--under <parent>]` 每条记录 → 一棵实例树（按名字填值、缺失留空）
- [x] `xr tmpl add/list/rm`（保护修订）、`xr instances`、`xr diff`（两文件对比）、`xr copy/fill/--no-history`
- [x] `xr-mcp`：MCP server（JSON-RPC over stdio，带 JSON Schema）
- [x] **MCP 与 CLI 对齐（0.2）**：抽出共享操作层（`rust/cli/src/ops.rs`）当唯一真相，两个入口都只是薄适配层；MCP 工具按领域重分组为 10 个（`context` / `file_info` / `file_validate` / `file_diff` / `tree` / `query` / `node` / `template` / `convert` / `blob`，动作型工具用 `action` 区分）；护栏统一（模板定义保护、危险操作要 `force`）；MCP 路径限定在 `--root` / `XIRANG_MCP_ROOTS` 允许目录内；本机状态（catalog / index / collection / compact / ws）仍只留 CLI
- [~] `find` 按类型 / 正则搜索、`match` 骨架 pattern 高级表达（待补）

### P5 — 写优化 / 跨库连接（后补，均已实现）

- [x] **XRIDX sidecar 索引**（实现层缓存 `<文件>.xirang.idx`，不改盘上格式）：根块 / 归属块 / 反向块 / 父子块，定长排序 + 磁盘二分；源文件尺寸或时间变了自动重建。对应错误码 F005–F008
- [x] **`shard-v1` 分片词库**：`xr collection split` 按判定器无损拆分、`xr collection list`、`xr compact` 折叠覆盖日志；写命令 `xr new/set/rename/rm/link/fill` 的 `<file>` 可直接传词库目录，只改目标分片。对应错误码 W001–W005
- [x] **`catalog-v1` 本机全局目录**：`xr catalog scan/list/check/forget/trash`；读写命令默认顺带维护（`--no-index` / `XIRANG_INDEX=off` 关闭）。对应错误码 W006–W008
- [x] **跨文件「同一节点」模型**：同编号出现在多个文件里不是冲突，各留各的孩子；`xr ws` 默认列出该编号的每一份、孩子取并集并标注来源文件，`--only` 只看单文件，`--json` 每项带 `source`；`xr catalog check [--sync <编号> --base <文件>]` 只同步节点自身的名字 / 值，孩子不动
- [x] **`xr index`**：重建 / 刷新 sidecar 索引并打印摘要

---

## 3. 渲染与样式（现状）

- **当前实现（2026-09 重做后）**：**Rust + egui** 立即模式界面，`app/src/`：`lazy.rs`（懒加载文档层）、`view.rs`（摊平成行 / 布局）、`edit.rs`（追加编辑 + 撤销栈）、`main.rs`（界面）。旧的 `app/dist` 与 `app/src-tauri`（Tauri + DOM/SVG）已删除，历史留在 git。
- **树视图**：虚拟化列表（只渲染可见的几十行），两种可切换布局 —— **横向缩进**（CLI 风，`├─`/`└─` 引导线）与**纵向分层**；未展开的节点显示 `▸ N` 徽标（N 来自侧车索引，不用先加载孩子）。
- **配色**：egui 内置暗 / 亮主题。
- **规模**：靠懒加载 + 虚拟化列表，百万级文件不整份载入；图谱（力导向、背景淡出）见 §10 的 M4。

---

## 4. CLI 命令清单（现状）

完整用法与例子见 [docs/CLI.md](https://github.com/outnever/XiRang/blob/main/docs/CLI.md)。

| 命令 | 作用 |
|------|------|
| `xr info` / `validate` | 摘要 / 校验（E/R） |
| `xr tree [--node <id>] [--skip-aux] [--ids] [--head N] [--depth N]` | 缩进树（可限条数 / 层数） |
| `xr cat [--head N] [--ids] [--skip-aux]` | 扁平视图：一行一个节点，按存放顺序 |
| `xr find <pattern> [--json]` | 按名字 / 文本值搜索 |
| `xr match --root\|--shape-of\|--template [--where 路径=值] [--json] [--tree]` | 按结构/名字/值匹配树 |
| `xr new` / `set` / `rename` / `rm` / `link` / `revert` | 增 / 改值 / 改名 / 删（置空）/ 连引用 / 回滚；**只追加写**（见下） |
| `xr copy [--blank] [--no-history]` | 复制子树（换新 UUID，内部引用重指） |
| `xr fill <root-id> <名/路径=值>… [--no-history]` | 按名字/路径赋值 |
| `xr export` / `import` | JSON / XML / YAML 无损往返；MD 有损导出 |
| `xr import <file> --template <name> <data.json> [--under <parent>]` / `xr import <file> --append <parent\|nil> <json>` | 按模板批量实例化 / 把嵌套 JSON 追加成子树 |
| `xr tmpl add\|list\|rm` | 模板管理（`add --from-json` 定结构；`rm` 需 `--yes`） |
| `xr instances <name>` | 列出某模板的实例 |
| `xr diff <a> <b> [--json]` | 两文件对比（增/删/改） |
| `xr refs` / `ws [--only <file>] [--json]` / `history` / `history prune [--keep N] [--dry-run] [--yes]` | 引用边 / 跨文件解析（默认取并集）/ 历史快照 / 裁剪留痕（真正删记录，故要 `--yes`） |
| `xr blob-import` / `blob-export` / `blob-info` | 二进制块 |
| `xr index status\|files\|update\|rebuild\|compact\|check\|gc\|drop\|forget\|unlock\|path` | 工作区台账维护（11 个子命令，全部支持 `--json`；`drop` / `forget` 要 `--yes`，任何子命令可 `--dry-run`） |
| `xr collection split\|list` | 分片词库：按判定器无损拆分 / 列出分片 |
| `xr compact <文件\|目录> [--all] [--dry-run]` | 折叠覆盖记录：`<文件>` 只折叠该文件内部同一编号的多份记录（不跨文件、不合并编号），`<目录>` 折叠各分片。不改变读得到的结果，故无 `--yes` |
| `xr catalog scan\|list\|check[ --sync <uuid> --base <文件>]\|forget\|trash` | 本机目录：扫描 / 列出 / 查自身不一致（可按基准文件同步名字与值）/ 移除 / 移入回收站 |

约定：数据走 stdout、错误/提示走 stderr；`--json` 结构化输出便于程序 / 大模型消费；`--no-history` 批量写入不留痕；`--yes` 跳过保护确认；读写命令默认维护本机目录（`--no-index` 关闭，`XIRANG_INDEX=off` 全局关闭）；`xr new/set/rename/rm/link/fill` 的 `<file>` 可传**词库目录**（只改目标分片）。

写路径约定（2026-09 起）：普通文件**只追加写**（append-v1）——改一个词只往文件末尾补几条记录，同一编号会攒下多份记录（读时取最后一条），首次编辑某根时幂等补一条 `@protocol = append-v1`；整份重写只留给 `import` 整文件替换、`history prune`、`tmpl rm`、压实。≥20 MB 文件的**索引登记与本机目录登记转后台**（命令先返回）。索引是缓存：格式版本对不上会直接报 F013 并回退整份载入，`xr index rebuild` 重建即可。

单节点写（`set` / `rename` / `rm` / `link`）**不整份载入**：按编号用台账直读那一条记录 → 改 → 只追加 → 幂等补声明 → 只登记新增段（`rust/core/src/edit.rs`，CLI 与桌面端**共用同一份实现**）。347 万节点的文件上改一个词约 **10 毫秒**（此前 3 秒，最初 13.2 秒）；台账还没有或与文件对不上时自动退回「整份载入」，结果一样。模板定义的护栏、`--no-history`、`--yes` 都在两条路上一致。

---

## 5. 跨文件引用设计（已由 catalog-v1 取代）

> 已由 `catalog-v1` 本机全局目录取代（实现层缓存，见 `spec/协议.md`、§2 的 P5）：跨库定位靠 `UUID → 文件`，`xr ws` 会自动回退查目录，并把**同一编号在多个文件里的孩子取并集**（同编号多文件不算冲突，各留各的孩子）。下面 `@address` 的持久化方案**不再采用**，保留作历史记录。

- 引用值本身仍是 16 字节 UUID（不改内核）。
- 运行时：维护全局索引，多文件打开时按 UUID 解析并连线。
- 持久化：目标文件未打开时，在引用节点下挂 `@address = "<目标文件路径>"`（或 `@source`）记录去向；文件打开后按 UUID 精确连上。
- 待定：是否把该约定写进 `spec/模板.md` 作为官方辅助节点用法。

---

## 6. 风险与待定项

- [x] **Rust 移植成本**：已完成——`rust/core` + `rust/cli`（`xr` / `xr-mcp`）就是交付实现，与 Python `tools/` 逐字节对拍通过。
- [ ] **文本渲染细节**：中文 / 混排、缩放、换行的绘制细节，需专门打磨（当前是 Rust + egui 即时模式绘制，字体与换行都自己控制）。
- [~] **大文件内存**：工作区台账（wsidx，默认）/ 侧车索引（XRIDX，回退）+ 按树懒加载已做；**mmap + 视口虚拟化**未做，需实测。
     桌面端常驻约 160 MB（179 MB / 347 万节点的文件）；CLI 的单节点写已改成按编号直读（约 10 毫秒），整份载入只剩导出 / 校验 / 需要整棵树的命令（347 万节点约 3 秒 / 近 1 GB）。
- [~] **追加写的代价**：同编号会攒下多份记录（文件慢慢变大），要靠 `xr compact <文件>` 收；已按裁定「不自动折叠、只提示」。
- [x] **跨文件引用的官方约定**：已定——由实现层 `catalog-v1` 承担（见 §5），不再用 `@address`。
- [x] **CLI 语言最终确定**：已定——Rust 单二进制 `xr` 为交付实现，Python `tools/cli.py` 保留作参考。
- [x] **格式对比实验**：已完成，数据在 `docs/格式对比.md`（见 §9）。

---

## 7. 里程碑（建议）

1. **M1**：Python CLI MVP（复用 `tools/`）跑通 P0 全部命令。（✅ 已完成，见 `tools/cli.py` + `tests/test_cli.py`）
2. **M2**：Rust 核心库完成 + 对拍测试通过。（✅ 已完成：`rust/core`（codec / validator / tree / convert / query / index / scan / shard / catalog / workspace）+ `rust/cli`（`xr` / `xr-mcp`）+ 与 Python 逐字节对拍。测试规模随后续开发增长，当前 Rust 121 条（17 个测试目标）+ Python 77 条 + 桌面端 23 条）
3. **M3**：桌面 P0（单文件树视图）。（🔄 已被 §10 的 d1/d2 取代：Tauri 版删除，改为 Rust + egui 重新实现）
4. **M4**：P1 编辑闭环（CRUD + 导出导入 + 撤销）。（🔄 被 §10 的 d3 取代）
5. **M5**：P2 多文件 + 图 + 引用。（🔄 被 §10 的 d4 取代）
6. **M6**：P3 性能 / 渲染 / 打磨，发布三平台。（🔄 被 §10 的 d5 取代；性能靠懒加载 + 虚拟化重做）
7. **M7**：P4 查询层 / 模板 / MCP（形状码索引、`@模板`/`@实例`、批量实例化、`xr-mcp`）。（✅ 已完成）
8. **M8**：P5 写优化 + 跨库连接（`shard-v1` 分片词库、`catalog-v1` 本机目录、工作区台账 `wsidx` + 侧车索引 XRIDX、跨文件同编号取并集）。（✅ 已完成）
9. **M9**：第 2 步写路径提速（台账只追加登记 `append_tail`、数据文件只追加写 `append-v1` 落地到 CLI、压实约束 + 改名）。（✅ 已完成，见「测试结果摘要 → 第 2 步」）

> 说明：勾选 `[x]` 表示**代码已实现**。桌面版（M3–M6）的功能虽然都写了，但「实际用起来对不对、顺不顺手」还需要你人工验收——那份清单在 `验收测试清单.md`，桌面版几节目前还没勾。

---

## 8. 性能目标（量级预估，精确值需实测）

三个量要分开看：

| 量 | 含义 | 目标量级 |
|----|------|---------|
| 同屏显示 | 一帧实际绘制的节点 | 1000–5000（可读上限；形状绘制技术上限 10 万级） |
| 内存内处理 | 常驻内存、可编辑 / 搜索 / 校验 | 100 万级稳妥，1000 万级需惰性 |
| 磁盘 / 流式 | 文件按需读 | 上亿级 |

- 单节点内存（Rust）约 150–400 字节，HashMap 索引每项另加 48–64B；100 万节点 ≈ 0.5 GB。
- 显示瓶颈是**文本**：LOD 分级（缩小画形状、放大画文字）；无边际画布 = 视口剔除。
- 极大文件策略：mmap → UUID→偏移稀疏索引 → 按需解析子树 → 视口虚拟化 → 只读操作流式化 → 1 亿+ 用磁盘索引（SQLite / RocksDB）。
- 图视图注意 O(N²) 边：需边剔除 / 聚合 / 悬停显示。
- 实测项（对齐 §6）：百万级遍历 / 校验耗时、万级文本渲染帧率、惰性解析内存曲线。

已落地的部分：工作区台账（wsidx）+ 侧车索引（XRIDX）让「按编号跳读」不必从头扫，跨文件按树懒加载、同编号读时取并集（P5）；写路径改成了**只追加**（改一个词只补几条记录，台账也只登记新增的那一段）；≥20 MB 文件的两本缓存（索引登记 / 本机目录登记）转后台。

**已实测（179 MB / 347 万节点，release）**：按编号点查 < 10 ms；桌面端改一个词 11 ms；CLI `xr set` 13.2 s → **约 10 ms**（按编号直读 + 只追加，不再整份载入）。整份载入（读 187 MB + 解码 347 万节点 ≈ 3 s）只剩导出 / 校验 / 需要整棵树的命令。

尚未落地：mmap、视口虚拟化。

---

## 9. 格式对比实验（已完成）

结论落在 **`docs/格式对比.md`**，README 的「息壤是什么」已按实测结果改写并链过去。文档按读者关心的**五个问题**组织（增删改查 / 空间与时间 / 用户便利性 / 大模型与 API 便利性 / 其他优劣），每个问题先给一句结论再给数据。

- **规模**：五个测试组（只给字节 / 文本格式 / 无专用工具可自写代码 / 带官方 CLI / 直接贴内容）× 四档规模（6 节点 → 347 万节点）× 六类任务（读全 / 查 / 增 / 改值 / 改名 / 删）× 5 个体量档模型，best-of-3，共 **1176 个格子，花费 $1.90**。
- **口径**：「不给工具」分三层——连代码都不让跑（只给字节）、不给息壤专用工具但可自写代码、给官方 CLI。用户平常说的「不给工具」通常指第二层。
- **体积**：数据一大，息壤约为 JSON 的 1/4；但 6 节点的小文件因为 2.4 KB 文件头反而比文本格式大。
- **只给字节（不能跑代码）**：只有 deepseek-v4-flash 能还原，实用上限约几十 KB；原来「9B 以上无需任何解析工具」的写法**与实测不符**，README 已改正。
- **可自写代码 / 给官方 CLI**：9B 模型自己写代码即可在小文件上满分。
- **增删改查**：查 / 增 / 改值 / 改名 / 删 都能做对。测的时候 `xr` 还没有改名命令（那是当时的短板），**现已补上**：`xr rename`（见 §4 命令表）。
- **网页端不可用**：网页版助手传不了二进制文件；转 base64 后 token 只有 JSON 的一半，但准确率大幅下降（S2 只还原 48%，S1 为 0）。README 已写明。
- **效能趋势**：能执行代码时花费与时间几乎不随文件变大而增长（按需读）；只给字节则线性增长、注定只能用于小文件。
- **引擎写代价**：改一处两边都要整份重写，但息壤文件小 4 倍，S4（179 MB）上 3.1 s vs JSON 17.8 s。
- 主图 `docs/格式对比图.svg` 入库；实验脚本与原始数据在开发机 `.exp/format-compare/`（不入库）。

### 待办（本轮发现）

- [x] `xr` 增加 `rename`（改名）命令：已实现——`xr rename <file> <node-id> <新名字> [--no-history] [--yes]`，支持词库目录；核心库补了 `Store::rename_quiet`。
- [x] `xr tree --head N`：已实现（提示走 stderr，stdout 保持干净）；MCP 侧对应能力并入 `tree`（`layout=flat` + `limit`，默认 200）。
- [x] **MCP / CLI 同步**：MCP 曾漏掉写操作与模板保护，并静默忽略未知参数（如 `import_template` 的 `under`）；已由共享操作层根治——能力、护栏、错误都出自同一份实现，并补了进程级 MCP 测试（工具清单 / 路径策略 / 护栏 / 与 CLI 的结果一致性）。

### 参数索引整改（工作区统一索引 + 对比基准）

- [x] **wsidx-v1**：工作区级三本台账（定位 / 关系 / 反向），`<工作区>/.xirang-index/`，每本 = manifest + 有序块（每块 100 万条）+ 追加日志；未知段可跳过
- [x] **后端抽象**：`index::Backend`（locate / children / references / subtree）+ 三个实现（`workspace` / `sidecar` / `memory` 回退），`XIRANG_INDEX_MODE=workspace|sidecar` 切换，默认工作区台账
- [x] **写路径**：数据落盘后自动追加索引日志；`xr index status|rebuild|compact|check|gc`；侧车模式不再生成 `.idx`
- [x] **测试**：新增 `core/tests/index_parity.rs`（三种后端在四类查询上逐条一致 + 索引覆盖率 + 日志覆盖层 + 压实后一致）
- [x] **对比基准**：本轮用一次性基准工具（`xr-bench gen|run|report` + 确定性词典型夹具生成器）跑出，覆盖点查 / 整树 / 反向 / 父子 / 跨文件 / 建索引 / 追加 / 压实 / 规模曲线 / 内存 / CLI 与 MCP 入口。**这些测试脚手架不随仓库发布**（见「测试结果摘要」一节），只保留结论
- [x] **实测结论（100 万节点 / 445 文件）**：工作区台账点查 31 µs（侧车 2593 µs，**84×**）、孩子 73 µs（2819 µs）、反向 20 µs（1906 µs）、整树 180 µs（11136 µs）；代价是建索引约 2×（555 ms vs 282 ms）、索引体积 1.24×（124 MB vs 100 MB）、单条写入追加该文件全部条目（8.8 ms vs 0.9 ms），因此需要定期 `xr index compact`
- [x] **收尾（可靠性 + 自动整理 + 入口开销）**：索引打开时发现块缺失即报 F012 并回退整份载入（不再静默返回空）；块文件带代数前缀，压实「新块 → 换 manifest → 删旧块」，扫描不持锁、改动即重试；`xr index compact` 之外，读/写命令结束后按阈值**分离后台进程**自动压实（`XIRANG_INDEX_MAINTENANCE=off` 关闭），MCP / 桌面版用后台线程；`xr ws` 不再把命令行点名的文件逐个读一遍去登记本机目录
- [x] **入口实测**：`xr ws`（100 万节点 / 445 文件）从约 90 ms 降到 **7.6 ms**（此前瓶颈是「逐文件 canonicalize + 逐文件登记目录」两处 O(N) 叠加）
- [ ] **待做**：1000 万节点档（1e7）与分片形态（`--kind shards`）的实测尚未跑，容量红线仍是外推值

### 索引管理命令面（11 条）

- [x] `xr index` 扩成完整一套：`status --verbose` / `files --stale` / `update`（增量自愈 + gc）/ `rebuild` / `compact` / `check --sample N --deep` / `gc` / `drop --yes` / `forget --yes` / `unlock` / `path`，全部支持 `--json`（camelCase，供脚本与 GUI）
- [x] 破坏性操作统一护栏：无 `--yes` 只打印「会删什么、影响多少」并退出码 2；`--dry-run` 先预演不动手；`drop`/`forget` 绝不触碰 `.xirang` 数据文件
- [x] 内核补 `info` / `files_status` / `update` / `drop_index` / `forget_files` / `unlock`；`status` 增加每本台账的条目数、索引目录总体积与协议版本
- [x] 测试：`core/tests/index_admin.rs`（外部改动 → 标记 → update 自愈、drop 只删索引、forget 保留数据、unlock 报 pid）与 `cli/tests/index_admin.rs`（护栏 / JSON / path / forget 端到端）全绿
- [ ] **待做**：GUI 侧消费 `xr index status --json`（本轮只保证 JSON 稳定）

### 留痕裁剪（history prune）

- [x] 内核 `Store::prune_history(node, keep, before)`：保留最近 N 条 `@history` 快照，可选只裁早于某 ISO 前缀的；被裁的是真正的结构删除（`@history` 本身保留）
- [x] CLI `xr history prune <file> <node-id> [--keep N] [--before <ISO前缀>] [--dry-run] [--yes]`：无 `--yes` 只报「会丢掉几条」并退出码 2；预演不动文件；带 `--yes` 才裁
- [x] MCP `node` 新增 `prune_history`（同一份 ops 实现，护栏走 `force`），保持两个入口一致
- [x] 实测收益：同一节点改 30 次的文件 6350 字节 → 裁到保留 5 条后 3243 字节（-49%）；`--keep 0` → 2618 字节
- [x] 不做「按编号去重/折叠」类命令：同一编号出现在多个文件里是**跨文件身份**的保证，任何形似去重的命令都会被误解，风险大于收益
- [x] **Python 参考实现同步**：`tools/tree.py` 补 `prune_history(node, keep, before)` 与 `_remove_subtree`，语义与 Rust 逐条对齐（保留最近 N 条、`before` 与 `@replaced` 比较字符串、真正结构删除、@history 本身保留）；`tests/test_history_prune.py` 含一条**跨语言校验**（Python 裁过的文件交给 Rust 读、校验并数快照）。参考实现不实现实现层缓存（索引）——那部分只对齐数据语义
- [x] CLI「看得舒服」三件套：`xr tree --depth N`（限层看骨架）、`xr cat`（扁平视图，一行一节点 + 10 万节点护栏）、`tree`/`cat` 在终端下自动分页（`$PAGER`，默认 `less -R`；`--no-pager` 关闭；管道不受影响）。不引入新依赖，也不做交互式编辑器。
- [~] **做一个顺手的息壤查看器**。CLI 侧已完成（`xr tree` 可限层/限条 + 自动分页、`xr cat` 扁平视图 + 大文件护栏）；桌面版待定。目标是两种视图看同一份数据：
  - **树视图**：现在的结构展示，适合看层级与引用。
  - **纯文本视图**：外观贴近系统自带的文本编辑器（一屏一行、可滚动 / 搜索 / 复制），每行是「节点名 = 值」，让人不必先理解格式就能上手。
  - 只读优先（先解决「看得见」），编辑后置；跨平台（复用 `xirang-core` 与现有 `app/` 骨架）。

## 10. 桌面端 v1 重做（2026-09）

> 起因：旧桌面端（Tauri + 手写 DOM）是「整份载入 + 全量重绘」，347 万节点的文件打不开、改一个词要 20.5 秒。
> 重做后的路线：**Rust + egui 原生界面 + 同进程调用 core；读走懒加载；写走单文件追加**。

### 进度

- [x] **d1 地基**：`append-v1` 协议（同编号多记录 = 修订，后写覆盖）+ 折叠读 + 侧车索引增量补扫（XRIDX 3）+ 追加写入器 + 合并；CLI 与 Python 参考实现同步；E002 语义收窄、新增 F015（尾部残片；F012–F014 已由主线的索引改动占用）。
- [x] **d2 能看**：egui 骨架 + 中文字体探测 + 懒加载文档层 + 虚拟化树列表 + 两种布局（横向缩进 / 纵向分层）+ 展开徽标 + 辅助节点开关。
- [x] **d3 能改**：改名 / 改值 / 新增 / 删除（置空槽位）+ 即时追加落盘 + 完整撤销 / 重做 + 折叠视图校验（E/R）。搜索、`@history` 时间线面板待做。
- [~] **d4 能连**：引用力导向图已接入 —— 参数照搬 Obsidian（向心力 0.1 / 排斥力 10 / 连线力 1 / 连线长度 250 / 文字淡入阈值 −3…+3 / 节点大小 / 连线粗细 / 箭头 / 生长动画 / 孤立节点开关）、标签按缩放淡入、悬停高亮邻居并显示「节点名 + 所在文件」、拖动节点时唤醒模拟（`alphaTarget 0.3`）、**聚焦子树改树布局 + 其余节点淡出为背景**。图设置面板可调 + 一键重置。**未做**：跨文件取并集（多文件工作区）。
- [x] **d5 打磨**：多文件工作区（跨文件引用成一张网）· 最近文件 · 只读模式 · 搜索（名字 / 值 / 类型，流式扫描）· 校验改为流式（不整份载入）· `@history` 时间线与一键回滚 · blob 预览与导出 · 导出 JSON / XML / YAML / Markdown · 手动合并入口 · 视图态持久化（`~/Library/Application Support/XiRang/views.json`）· macOS 打包（`scripts/make_macos_app.sh` 出 `dist/XiRang.app`，带 `.xirang` 文件关联）。

- [x] **d6 收尾**：中英界面切换（`i18n.rs`，带"漏翻就红"的守护测试）· 自定义配色（暗 / 亮预设 + 背景 / 前景 / 强调色 / 辅助色 / 边框，写进用户配置）· 图片预览（blob 里的 PNG / JPEG 直接画出来，换节点即释放纹理）· 导出范围三选一（完整折叠视图 / **当前视图（含折叠态）** / 选中子树）。

**仍未做（只剩跨平台）**：Windows / Linux 的适配与打包（代码本身跨平台，缺打包脚本与真机验证）· 中英之外的其它语言。

### d7 图谱按 Obsidian 逐行复刻（2026-09）

把参考实现（`obsidian-graph-lab.source.html`，即 Obsidian `sim.js` + canvas 渲染器的移植版）当规格，
在 `app/src/obsidian/` 里用 Rust 重写，替换掉原先那个自研力导向图（`graph.rs` 已删除）。

- **物理**：d3-quadtree（`cover`/`add`/`visit`/`visitAfter`，含重合点链表与 do-while 分裂）+ 五力固定顺序
  （中心 → 连线 → 排斥 Barnes–Hut → 碰撞 → 积分）；`velocityDecay 0.6`、`alphaDecay = 1 − 0.001^(1/300)`、
  `theta² 0.81`、`distanceMin 30`、碰撞半径 60 / 强度 0.5、`strength = linkStrength/min(度A,度B)`、
  `bias = 度A/(度A+度B)`；滑杆映射 `h1(v,t) = (t^(1−v) − t)/(1−t)`（t = 0.01）与 `排斥力 = v³`。
- **对拍证据**：用 Node 跑参考实现生成 golden（60 节点/120 步、400 节点/60 步，同一 LCG 序列），
  Rust 侧复现 → 最大偏差 **8.0e-14 / 2.5e-13 世界单位**（坐标量级数百，等于浮点级一致）。
- **渲染**：连线按（颜色 + 1/24 量化透明度）分批、端点按半径裁短、视口裁剪；节点圆半径 `getSize × √scale`；
  高亮环 `lw = max(1, √scale)`；箭头四点多边形 + `clamp(2(scale−0.3),0,1)` 淡入、双向边只画一支；
  标签居中挂在节点下方、`fontPx = max(9,(14+size/4)×√scale)`、透明度 `clamp(log2(scale)+1−fade,0,1)`、
  高亮时下移 15px、光晕用 **8 向偏移 1.5px** 模拟 canvas 的 `strokeText`。
- **交互**：滚轮以光标为锚点缩放（缩小回视图中心）、拖空白平移 + 惯性（`panX += 1000·panvX/60` 后逐帧衰减）、
  拖节点用 `fx/fy` 钉住 + `alphaTarget 0.3`（松手清空 fx/fy）、布局收敛期 `fitScale`（95% 分位）、
  悬停高亮 + 邻居压暗 `NU 0.2`、**静止 60 帧后停止重绘**、重置视图。
- **数据映射**：引用边取自侧车索引（与展开状态无关，跨文件取并集）；未解析 = 引用断裂（R001）；
  附件 = 二进制块节点；标签 = `@` 辅助子节点（按名字聚合成标签节点）；孤立节点 = 已加载但无边的节点；
  局部图谱 = 焦点 1 跳（中心权重 30、1 跳 0）；颜色分组按「所在文件」哈希到 6 色；
  上限：2 万条边 / 2000 个孤立节点，超出截断并在面板提示。
- **物理跑在后台线程**（等价于参考的 Worker + 版本号双缓冲），主线程只渲染；静止后线程进入低频轮询。

**已知差异（诚实说明）**：
1. **文字**：egui 没有 `strokeText`，光晕是 8 向偏移模拟；字重取决于加载到的字体——
   现在会优先找 Inter（放进 `.app/Contents/Resources` 或 `~/Library/Fonts` 即生效，OFL 许可），
   找不到就用系统的 Noto Sans SC / Hiragino。**Inter 尚未下载**（需要一次联网授权）。
2. 画布单位用「点」而不是 canvas 的设备像素（`dpr` 固定为 1）——egui 本身按 DPI 渲染，观感一致，
   但若逐像素比对截图会有细微差别。
3. 颜色分组只按「文件」分色（参考里是 `path:` / `tag:` / `file:` 三种查询前缀 + 布尔运算），
   没有查询编辑器。

### 大文件实测（179 MB / 347 万节点）

| 动作 | 用时 | 说明 |
|------|------|------|
| 冷开 | 0 ms | 只读索引，不载入节点 |
| 取一个节点 | 16 µs | 查目录 → 跳字节 → 解一条 |
| 改一个词 | 11 ms | 追加一条记录 + 索引补扫 |
| 流式搜索 | ~220 ms | 整份扫一遍，内存只放命中集 |
| 流式校验 | ~1.1 s | 只用一张「编号 → 父节点」表，不构造整棵 Store |
| 对照：整份载入 | 3.46 s / 约 1 GB | 只在导出、合并时按需走 |
| 打开文件后常驻内存 | 约 161 MB | 打包版实测 |

### 内存策略（不需要就及时还回去）

所有"吃内存"的部分都有明确释放点，不靠"程序退出才回收"：

| 资源 | 上限 / 释放点 |
|------|--------------|
| 节点缓存（懒加载读过的节点） | 2 万条上限；**关文件**、**切视图**、**切到图视图**、**空闲 6 秒**都会压缩或清空；工具栏有「释放缓存」按钮可手动清 |
| 图数据（节点 / 边的位置与速度） | 离开图视图即整体释放（`release_graph`），不留在内存里 |
| 树行列表（摊平结果） | 切到图视图时释放并 `shrink_to_fit`；最多 5 万行 |
| 校验用的整份载入 | 只在点「校验」时构造，函数返回即释放（局部变量） |
| 侧车索引 | 只保留内存里的修订块（编辑产生的增量），四个基础块始终在磁盘上按需 seek |

实测：打开 179 MB / 347 万节点文件，常驻内存约 **163 MB**（旧的「整份载入」路线约 1 GB）。

### 关键实测（179 MB / 347 万节点）

| 动作 | 改造前 | 改造后 |
|------|--------|--------|
| 打开 | 4.3 s（整份载入，约 1 GB 内存） | 0 ms（只读索引，不载入节点） |
| 取一个节点 | — | 16 µs（查目录 → 跳字节 → 解一条） |
| 改一个词 | 20.5 s（重写 179 MB + 重建 318 MB 索引） | 11 ms（追加一条记录 + 索引补扫） |
| 对照：整份载入 | 4.3 s | 3.46 s（仅导出 / 校验时按需走这条路） |

### 仍未做（说清楚，避免误解）

- 力导向图、聚焦态背景淡出、跨文件工作区尚未接入界面（模型与取数接口按 §2 的 P2 已有，界面在 d4）。
- 视图态持久化（`~/Library/Application Support/XiRang/views.json`）未做，目前折叠态与布局选择只活在会话内。
- 合并（compact）：CLI 与桌面端都已有（`xr compact <文件>` / 工具栏「合并」），
  2026-09-26 起输出「记录多少 → 多少、字节多少 → 多少」，并支持 `--dry-run`。

## 测试结果摘要（本轮索引改造，2026-09）

> 测试脚手架（一次性基准工具 `xr-bench`、夹具生成器、以及为此写的测试文件）**不随仓库发布**；
> 原始数据与图表留在开发机的 `.exp/index-compare/`。下面只留结论，供后续决策引用。

**双索引对比**（词典型夹具：一个文件多棵树 + 继承树 + 跨文件引用；规模 1 万 / 10 万 / 100 万节点）

| 场景 | 工作区台账 | 每文件侧车 | 说明 |
|---|---|---|---|
| 点查（任意编号） | 30 µs | 2556 µs | 100 万节点 / 445 文件时 **85×**；5 个文件时两者持平 |
| 父子（孩子并集） | 69 µs | 2755 µs | 40× |
| 反向（谁引用我） | 20 µs | 1925 µs | 99× |
| 整树读取 | 169 µs | 10969 µs | 65× |
| 建索引 | 553 ms | 276 ms | 侧车快 2× |
| 索引体积 | 124 MB | 100 MB | 台账 ≈ 数据的 **2.4 倍**（数据 51 MB） |
| 单条写入 | 6.7 ms | 0.9 ms | 台账每次追加该文件全部条目，需定期压实 |
| 压实 | 567 ms | — | 台账独有 |

关键规律：**侧车是 O(文件数)，台账是 O(1)**——文件越多差距越大。整份载入（内存内）查询 8–28 µs，是性能上界，但启动要 115 ms / 100 万节点、常驻约 110 MB。

**入口开销**：100 万节点 / 445 文件时 `xr ws` **7.6 ms**（优化前约 90 ms，瓶颈是逐文件 `canonicalize` + 逐文件登记本机目录）；MCP 常驻会话里一次查询 0.4 ms。

**留痕代价与裁剪**：同一节点改 30 次 → 文件 6350 字节、63 个节点；`xr history prune --keep 5` → 3243 字节（**-49%**），`--keep 0` → 2618 字节。改 50 次对比：留痕 8850 字节 / 103 节点，`--no-history` 2498 字节 / 1 节点（每改一次 +2 节点）。

**与其它存储的对照**（同一份 30 万节点数据）：息壤台账点查 27 µs、孩子 65 µs；SQLite 建索引后点查 7.6 µs、单条更新 14.8 µs（快 460×）；JSON 需整份载入 128 ms 才能查到。体积：息壤数据 15.3 MB / JSON 32 MB / SQLite 63 MB。定位：**比纯文本文件强两个数量级，比嵌入式数据库弱在写入与聚合**。

**测试规模**：Rust 105 条（CLI / MCP / 内核 / 对拍 / 故障演练 / 管理命令）+ Python 73 条，全绿；CI 跑 `cargo test` 与 `pytest`。

**尚未实测**：1000 万节点档与分片形态（`--kind shards`）未跑，容量红线仍是外推值。

### 第 2 步（单文件写入提速）—— 已完成主体（2026-09-26）

**目标**：187 MB / 347 万节点的文件，「改一个词」不再与文件大小成正比。

**已完成（分支 `codex/app-editor`）**：
- 护栏取回：`core/tests/index_parity.rs`（三后端四类查询逐条一致）、`index_faults.rs`（坏索引必须报错）、`fixture.rs`
- `Store::save` 改成原子写（临时文件 + 改名）——压实整份重写时崩掉不再损坏库
- `Reader::locate` 去重口径改成「**编号 + 文件**、日志优先」（追加写落地的前提）
- 大文件的索引登记改到**后台进程**（`ops::update_index`，>20 MB 才启用；需 `xr` 可执行文件在旁边，桌面端会自动退回同步）

**这次做完的四块**：

1. **台账有效性规则**（`93ca799`，5.1）：条目属于「写它时那条文件记录」的代号。
   整份登记 → 代号 +1、旧条目作废（不变）；**只追加登记 → 代号一个字都不动**，旧条目继续有效。
   选的是**不加 mode 字段**的方案（台账文件格式一字不动）——代号比较本来就已经表达了这条规则。
2. **`wsidx::append_tail`**（`93ca799`，5.2）：只读「上次登记之后新增的那一段」并逐条解码，
   补定位 / 关系条目；父编号先在本批新增里找、再查台账；文件记录里加了
   `indexed_upto` + `prefix_guard`（起点前 64 字节哈希，长度不变也能抓住「从中间改写」）。
   对不上就返回 Err、退回整份登记，**绝不静默丢条目**。
   反向本加墓碑记录（`REC_TOMBSTONE`）：引用被改掉时旧边显式作废，否则「谁引用我」会一直列出旧来源。
3. **单文件只追加写**（`58adfb5`，5.3）：`Store` 加「写入日志」（只记碰过谁，不克隆整棵树），
   `shard::append_journal` 只补变化的那几条；首次编辑某根时**幂等**补 `@protocol = append-v1`；
   物理删除（`history prune` / `tmpl rm`）退回整份重写。本机目录登记（4–6 秒）也挪到后台。
4. **压实三条约束 + 改名**（`e837736`，5.4）：`index::compact_file` → `tree::fold_file`
   （折叠的是数据文件、不是索引），加 `tree::fold_prepare` 预演；输出报「记录多少 → 多少、字节多少 → 多少」；
   支持 `--dry-run`、不加 `--yes`；没有重复记录时一个字节都不写。

**当前实测**（187 MB / 347 万节点，release，默认设置 = 后台整理开着）：

| 动作 | 之前 | 现在 |
|---|---|---|
| `xr set` 改一个词（台账 + 本机目录） | 13.2 s | **2.9–3.3 s**（温缓存）/ 4.0–4.3 s（刚复制完、冷缓存） |
| 同上，关掉本机目录（`XIRANG_INDEX=off`） | 9.6 s | **2.9 s** |
| 台账日志每次写多少 | 整份重扫，300+ MB | **约 10 KB** |

剩下的 2.9 s **全是「整份载入」**（读 187 MB + 解码 347 万节点）。要再往下压，得做第四块：
按编号用台账直读那一个节点、不再整份载入——也就是桌面端已经在做的那件事（见下）。

**仍未做**：

**第四块 · 单节点直读编辑（已完成，2026-09-26）**：`rust/core/src/edit.rs` 把
「按编号取一条 → 改 → 只追加 → 幂等补声明 → 只登记新增段」抽成一份实现，
CLI（`set` / `rename` / `rm` / `link`）与桌面端（`app/src/edit.rs` 的 `Editor`）
都调它；桌面端的撤销栈保留（那是界面里的临时功能，与数据里的 `@history` 是两回事），
索引仍由 `xirang_cli::ops::update_index` 这一个入口维护。

实测：CLI 改一个词 3 s → **约 10 ms**；桌面端 11 ms（两条路现在共用同一份落盘规则）。
护栏：`core/tests/edit_path.rs`（4 条：与整份载入改出来的树一致 / 没台账必须拒绝 /
连改多次声明仍只有一条且台账跟得上 / 模板定义识别）、`cli/tests/append_write_min.rs`
（走快路与退回整份载入各一条）、`app/tests/edit_shared.rs`（桌面端与 CLI 同一套语义）。

**必跑护栏**：`index_parity` / `index_faults` / `backend_parity` / `append_v1` /
`index_append_tail`（5 条）/ `edit_path`（4 条）/ `append_write_min`（3 条）/ `edit_shared`（1 条）/
`cli.rs::catalog_same_uuid_is_normal_union_and_only`（同一编号多文件必须报「2 处」）+ 全量 `cargo test` + `pytest`。
