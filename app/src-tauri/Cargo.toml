[package]
name = "xirang-app"
version = "0.1.0"
edition = "2021"
description = "息壤阅读编辑工具（桌面版，Tauri）"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xirang-core = { path = "../../rust/core" }
